{{#if @amp.shareSystem}}
    <amp-social-share
            type="system"
            width="40"
            height="40"
            data-param-text="{{title}}">
    </amp-social-share>
{{/if}}

{{#if @amp.shareFacebook}}
    <amp-social-share
            type="facebook"
            width="40"
            height="40"
            data-param-app_id="{{@amp.shareFacebookId}}"
            data-param-text="{{title}}"
            data-param-href="{{url}}">
    </amp-social-share>
{{/if}}

{{#if @amp.shareTwitter}}
    <amp-social-share
            type="twitter"
            width="40"
            height="40"
            data-param-text="{{title}}"
            data-param-url="{{url}}">
    </amp-social-share>
{{/if}}

{{#if @amp.sharePinterest}}
    <amp-social-share
            type="pinterest"
            width="40"
            height="40"
            data-param-url="{{url}}">
    </amp-social-share>
{{/if}}

{{#if @amp.shareLinkedin}}
    <amp-social-share
            type="linkedin"
            width="40"
            height="40"
            data-param-text="{{title}}"
            data-param-url="{{url}}">
    </amp-social-share>
{{/if}}

{{#if @amp.shareTumblr}}
    <amp-social-share
            type="tumblr"
            width="40"
            height="40"
            data-param-text="{{title}}"
            data-param-url="{{url}}">
    </amp-social-share>
{{/if}}

{{#if @amp.shareWhatsapp}}
    <amp-social-share
            type="whatsapp"
            width="40"
            height="40"
            data-param-text="{{title}}">
    </amp-social-share>
{{/if}}